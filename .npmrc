# Use prebuilt binaries where available
sqlite3_binary_host=https://github.com/mapbox/node-sqlite3/releases/download/
sqlite3_binary_host_mirror=https://mapbox-node-binary.s3.amazonaws.com/

# Skip building native modules if prebuilt binaries are available
npm_config_build_from_source=false

# Set Python path (if needed)
# npm_config_python=C:\Python313\python.exe

# Set MSBuild path (if needed)
# npm_config_msvs_version=2019